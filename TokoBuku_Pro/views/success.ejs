<!DOCTYPE html>
<html>
<head>
    <title>Struk Pembelian</title>
    <style>
        body { font-family: 'Courier New', Courier, monospace; background-color: #e0e0e0; display: flex; justify-content: center; align-items: center; min-height: 100vh; margin: 0; }
        .receipt { background: #fff; width: 320px; padding: 20px; box-shadow: 0 4px 10px rgba(0,0,0,0.1); position: relative; }
        .header { text-align: center; border-bottom: 2px dashed #333; padding-bottom: 15px; margin-bottom: 15px; }
        .row { display: flex; justify-content: space-between; margin-bottom: 8px; font-size: 0.9em; }
        .total-row { font-weight: bold; font-size: 1.1em; border-top: 2px dashed #333; padding-top: 10px; margin-top: 10px; }
        .api-key-box { background: #f0f0f0; padding: 10px; text-align: center; font-weight: bold; color: #d32f2f; border: 1px solid #ccc; margin: 15px 0; word-wrap: break-word; }
        .btn-back { display: block; width: 100%; text-align: center; background: #333; color: white; text-decoration: none; padding: 10px 0; margin-top: 15px; font-family: sans-serif; font-weight: bold; }
    </style>
</head>
<body>
    <div class="receipt">
        <div class="header"><h2 style="margin:0;">TOKO Grecia</h2><p style="margin:5px 0 0; font-size:0.8em; color:#555;">BUKTI TRANSAKSI DIGITAL</p></div>
        <div class="details">
            <div class="row"><span>Tanggal:</span><span><%= new Date(resi.date).toLocaleDateString() %></span></div>
            <div class="row"><span>Waktu:</span><span><%= new Date(resi.date).toLocaleTimeString() %></span></div>
            <div class="row"><span>Pembeli:</span><span><%= pembeli %></span></div>
            <br><div class="row"><span>ITEM:</span></div><div class="row"><span><%= resi.book_title %></span></div>
            <div class="row total-row"><span>TOTAL:</span><span>Rp <%= Number(resi.price).toLocaleString('id-ID') %></span></div>
        </div>
        <div style="text-align: center; font-size: 0.8em; margin-bottom: 5px;">KODE AKSES ANDA:</div>
        <div class="api-key-box"><%= resi.api_key %></div>
        <a href="/" class="btn-back">Kembali Belanja</a>
    </div>
</body>
</html>